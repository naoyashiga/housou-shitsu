section.mdl-grid
  .mdl-cell.mdl-cell--center.mdl-cell--9-col
    .mdl-card.mdl-shadow--2dp
      .mdl-card__title.mdl-typography--headline 放送リスト
      .mdl-card__supporting-text
        | {{ paginator }}

        a(href="{{ paginator.previous_page_path }}") previous
        ul
        | {% assign offset = 20 %}
        | {% for post in site.categories.archive reversed %}
        | {% assign loopindex = forloop.index | modulo: offset %}
        | {% if loopindex == 0 %}
        | {% assign nextIndex = forloop.index | plus: offset %}
        li
            | {{ post.title }} - vol.{{ nextIndex }}
        | {% endif %}
        | {% endfor %}

| {% assign offset = 10 %}
| {% for post in paginator.posts reversed %}
//- | {% for post in site.categories.archive reversed %}
| {% assign loopindex = forloop.index | modulo: offset %}
| {% if loopindex == 0 %}
| {% assign nextIndex = forloop.index | plus: offset %}
section.mdl-grid
  .mdl-cell.mdl-cell--center.mdl-cell--9-col
    .mdl-typography--headline 放送リストvol.{{ forloop.index }}~{{ nextIndex }}
| {% endif %}
section.mdl-grid
  .mdl-cell.mdl-cell--center.mdl-cell--9-col
    table.mdl-data-table.mdl-js-data-table.mdl-shadow--2dp
      thead
        tr
          th vol
          th タグ
      tbody
        tr
          td
            a(href='{{ post.url }}') vol.001
          td
            ul.hs-list
              | {% for tag in post.tags %}
              li.hs-list-item
                span.mdl-chip.hs-chip
                  a.mdl-chip__text(href='/lists/tag/{{ tag | slugify }}') {{ tag }}
              | {% endfor %}
              | {% for tag in post.tags %}
              li.hs-list-item
                span.mdl-chip.hs-chip
                  a.mdl-chip__text(href='/lists/tag/{{ tag | slugify }}') {{ tag }}
              | {% endfor %}
              | {% for tag in post.tags %}
              li.hs-list-item
                span.mdl-chip.hs-chip
                  a.mdl-chip__text(href='/lists/tag/{{ tag | slugify }}') {{ tag }}
              | {% endfor %}
        tr
          td vol.002
          td 50
    .mdl-card.mdl-shadow--2dp
      .mdl-card__title.mdl-typography--title {{ post.title }}
      .mdl-card__supporting-text

      .mdl-card__actions.mdl-card--border
        a.mdl-button.mdl-button--raised.mdl-js-ripple-effect.mdl-button--colored.hs-button(href='{{ post.url }}')
          | 詳細をみる
        a.mdl-button.mdl-button--raised.mdl-js-ripple-effect.mdl-button--accent.hs-button(href='https://www.youtube.com/results?search_query=放送室+{{ post.youtube }}' target="_blank")
          | Youtubeで探す
      .mdl-card__actions.mdl-card--border
        | {% for tag in post.tags %}
        span.mdl-chip.hs-chip
          a.mdl-chip__text(href='/lists/tag/{{ tag | slugify }}') {{ tag }}
        | {% endfor %}
| {% endfor %}
